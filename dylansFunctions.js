const add = (num1, num2) => {
  return num1 + num2
}

const subtract = (num1, num2) => {
  return num1 - num2
}

module.exports = {
  name: "dylansFunctions.js",
  desc: "Helper functions to help you do things",
  add,
}